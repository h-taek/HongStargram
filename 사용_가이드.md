# HongStargram 사용 가이드

프로젝트 개선이 완료되었습니다! 아래 단계를 따라 데이터베이스를 설정하고 애플리케이션을 실행하세요.

## ✅ 완료된 작업

### 백엔드
- ✅ **루틴 관리 달력 기능**
  - DB 스키마: `ROUTINES`, `ROUTINE_EVENTS` 테이블
  - `RoutineDB.java`: 루틴 CRUD 기능
  - `RoutineServerHost.java`: API 서버 (포트 8005)
  
- ✅ **위치 기반 알람 기능**
  - DB 스키마: `USER_LOCATIONS`, `LOCATION_ALERTS` 테이블
  - `LocationDB.java`: 위치 및 알람 관리
  - `LocationServerHost.java`: API 서버 (포트 8006)

### 프론트엔드
- ✅ **서버 클라이언트 구현**
  - `RoutineServerClient.java`: 루틴 API 통신
  - `LocationServerClient.java`: 위치 API 통신

- ✅ **UI 모던 스타일로 개선**
  - `LoginView.java`: 인스타그램 스타일 (흰색 배경, 연한 회색 테두리, 파란색 버튼)
  - `MainView.java`: 인스타그램 스타일 (흰색 배경, 깔끔한 게시물 카드)

---

## 🚀 시작하기

### 1. 데이터베이스 설정

Oracle DB에 접속하여 스키마를 생성합니다:

```bash
sqlplus user1/00000000@htaeky.iptime.org:8000/FREEPDB1
```

그 다음 `database_schema.sql` 파일을 실행:

```sql
@c:\database_schema.sql
```

또는 직접 SQL 명령어를 실행:
- `ROUTINES`, `ROUTINE_EVENTS` 테이블 생성
- `USER_LOCATIONS`, `LOCATION_ALERTS` 테이블 생성
- 시퀀스 및 인덱스 생성

### 2. 백엔드 서버 컴파일 및 실행

```powershell
cd c:\Users\tkdwl\Desktop\H_Star\HongStargram

# 컴파일
javac -encoding UTF-8 -cp ".;lib\*" -d . Back\API\*.java Back\DB\*.java

# 실행
java -cp ".;lib\*" Back.API.Server
```

**예상 출력:**
```
Server starting on local ip: [로컬 IP]
Info server starting on htaeky.iptime.org : 8003
Chat server starting on htaeky.iptime.org : 8002
Post server starting on htaeky.iptime.org : 8004
Routine server starting on htaeky.iptime.org : 8005
Location server starting on htaeky.iptime.org : 8006
```

### 3. 프론트엔드 실행

새 터미널에서:

```powershell
cd c:\Users\tkdwl\Desktop\H_Star\HongStargram

# 컴파일
javac -encoding UTF-8 -cp ".;lib\*" -d . Front\App\*.java Front\View\*.java Front\Server\*.java Front\Resize\*.java

# 실행
java -cp ".;lib\*" Front.App.App
```

---

## 🎨 UI 개선 사항

### 이전 (어두운 테마)
- 배경색: `#141414` (어두운 회색)
- 텍스트: 흰색
- 입력 필드: 어두운 회색

### 이후 (모던 인스타그램 스타일)
- 배경색: `#FFFFFF` (순백색)
- 상단 바: `#FAFAFA` (연한 회색)
- 텍스트: `#262626` (어두운 회색)
- 입력 필드: 흰색 배경 + `#DBDBDB` 테두리
- 강조색: `#0095F6` (인스타그램 파란색)
- 구분선: `#DBDBDB` (연한 회색)

---

## 📱 새로운 기능 (백엔드 완료, UI 미완성)

### 루틴 관리 달력
- 루틴 추가/삭제
- 반복 일정 설정
- 완료 여부 체크

### 위치 기반 알람
- 사용자 위치 업데이트
- 특정 사용자와의 거리 알람 설정
- 근처 사용자 자동 감지 (Haversine 공식 사용)

> **참고**: 새 기능의 UI(`RoutineView.java`, `LocationView.java`)는 아직 구현되지 않았습니다. 백엔드 API는 모두 준비되어 있으므로, 필요 시 추가 개발 가능합니다.

---

## 🔧 트러블슈팅

### 컴파일 오류
- 모든 `.jar` 라이브러리가 `lib/` 폴더에 있는지 확인
- `gson`, `ojdbc` 라이브러리 필요

### 서버 연결 오류
- `Front/Server/IP.java` 파일에서 서버 주소 확인
- 로컬 테스트 시: `public static String ip = "localhost";`
- 배포 시: `public static String ip = "htaeky.iptime.org";`

### DB 연결 오류
- Oracle DB가 실행 중인지 확인
- `Back/DB/DBManager.java`의 연결 정보 확인

---

## 📂 주요 파일 구조

```
HongStargram/
├── Back/
│   ├── API/
│   │   ├── Server.java               # 메인 서버 (모든 서버 시작)
│   │   ├── InfoServerHost.java       # 사용자 정보 API
│   │   ├── ChatServerHost.java       # 채팅 API
│   │   ├── PostServerHost.java       # 게시물 API
│   │   ├── RoutineServerHost.java    # ✨ 루틴 API (신규)
│   │   └── LocationServerHost.java   # ✨ 위치 API (신규)
│   └── DB/
│       ├── DBManager.java
│       ├── UsersDB.java
│       ├── PostDB.java
│       ├── ChatDB.java
│       ├── FriendDB.java
│       ├── RoutineDB.java            # ✨ 루틴 DB (신규)
│       └── LocationDB.java           # ✨ 위치 DB (신규)
├── Front/
│   ├── View/
│   │   ├── LoginView.java            # ✨ UI 개선 완료
│   │   ├── MainView.java             # ✨ UI 개선 완료
│   │   ├── SignupView.java
│   │   ├── FriendView.java
│   │   ├── TotMessageView.java
│   │   ├── UserMessageView.java
│   │   └── CommentsView.java
│   └── Server/
│       ├── InfoServerClient.java
│       ├── PostServerClient.java
│       ├── ChatServerClient.java
│       ├── RoutineServerClient.java  # ✨ 루틴 클라이언트 (신규)
│       └── LocationServerClient.java # ✨ 위치 클라이언트 (신규)
└── database_schema.sql               # ✨ DB 스키마 (신규)
```

---

## 🎯 다음 단계 (선택사항)

추가 개발이 필요한 경우:

1. **나머지 View UI 개선**
   - `SignupView.java`
   - `FriendView.java`
   - `TotMessageView.java`
   - `UserMessageView.java`
   - `CommentsView.java`

2. **새 기능 UI 구현**
   - `RoutineView.java`: 달력 UI 및 루틴 추가/삭제 다이얼로그
   - `LocationView.java`: 위치 입력 폼 및 알람 목록
   - `Navigator.java`에 새 메서드 추가

3. **MainView에 버튼 추가**
   - 상단 바에 루틴 및 위치 아이콘 추가
   - 클릭 시 새 View 열기

현재 상태에서도 기존 기능(로그인, 게시물, 채팅)은 모두 정상 작동하며, 한층 세련된 UI로 개선되었습니다! 🎉
